import { filterPokemonType } from '../src/data';

describe('filterPokemonType', () => {
  it('debería ser una función', () => {
    expect(typeof filterPokemonType).toBe('function');
  });

  describe('filterPokemonType', () => {
    // escribe aquí tu test
  });
});
